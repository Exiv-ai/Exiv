[project]
name = "exiv-mcp-cerebras"
version = "0.1.0"
description = "Exiv MCP Server: Cerebras ultra-high-speed reasoning engine"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
