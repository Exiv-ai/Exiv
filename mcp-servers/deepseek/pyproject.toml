[project]
name = "exiv-mcp-deepseek"
version = "0.1.0"
description = "Exiv MCP Server: DeepSeek reasoning engine"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
