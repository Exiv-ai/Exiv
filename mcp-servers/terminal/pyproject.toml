[project]
name = "exiv-mcp-terminal"
version = "0.1.0"
description = "Exiv MCP Server: Terminal (sandboxed command execution)"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[project.scripts]
exiv-mcp-terminal = "server:main"
